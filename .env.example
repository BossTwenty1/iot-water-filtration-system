# Copy this file to a local environment file only when implementation begins.
# Keep all values blank in the repository and never commit a real .env file.

# Frontend API URL (name is provisional until the frontend is initialized).
FRONTEND_API_BASE_URL=

# Backend/Supabase connection values (names are provisional).
SUPABASE_URL=
SUPABASE_ANON_KEY=

# Local backend port (provisional).
BACKEND_PORT=
