import type { ReactNode } from 'react'
export function PageHeader({title,description,badge,actions}:{title:string;description:string;badge?:string;actions?:ReactNode}) { return <div className="flex flex-col gap-4 md:flex-row md:items-end md:justify-between"><div><div className="mb-1 flex flex-wrap items-center gap-2"><h2 className="text-2xl font-bold tracking-tight text-ink md:text-[28px]">{title}</h2>{badge&&<span className="eyebrow rounded bg-blue-100 px-2 py-1 text-hydro">{badge}</span>}</div><p className="max-w-3xl text-sm text-muted">{description}</p></div>{actions&&<div className="flex flex-wrap gap-2">{actions}</div>}</div> }
