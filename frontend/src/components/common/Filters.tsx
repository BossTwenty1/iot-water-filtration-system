import { RotateCcw,SlidersHorizontal } from 'lucide-react'
import { useId } from 'react'
import type { ReactNode } from 'react'
export function FilterToolbar({children,onReset}:{children:ReactNode;onReset?:()=>void}) { return <div className="panel p-3"><div className="mb-3 flex items-center gap-2"><SlidersHorizontal size={16} className="text-hydro"/><span className="eyebrow font-semibold text-ink">Scientific filter console</span></div><div className="grid gap-3 sm:grid-cols-2 xl:grid-cols-[repeat(4,minmax(0,1fr))_auto]">{children}<div className="flex items-end gap-2"><button type="button" className="button-primary flex-1">Apply Filters</button>{onReset&&<button type="button" className="icon-button" onClick={onReset} aria-label="Reset filters"><RotateCcw size={16}/></button>}</div></div></div> }
export function SelectField({label,value,onChange,options}:{label:string;value:string;onChange:(value:string)=>void;options:string[]}) { const id=useId(); return <div className="field"><label htmlFor={id}>{label}</label><select id={id} name={label.toLowerCase().replaceAll(' ','-')} className="control" value={value} onChange={(event)=>onChange(event.target.value)}>{options.map((option)=><option key={option}>{option}</option>)}</select></div> }
