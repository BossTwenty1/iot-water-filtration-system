import type { LucideIcon } from 'lucide-react'
import type { ReactNode } from 'react'
export function SectionCard({title,description,action,children,className='',id}:{title?:string;description?:string;action?:ReactNode;children:ReactNode;className?:string;id?:string}) { return <section id={id} className={`panel min-w-0 ${className}`}>{(title||action)&&<div className="flex flex-wrap items-start justify-between gap-3 border-b border-slate-200 px-4 py-3"><div>{title&&<h3 className="text-base font-semibold text-ink">{title}</h3>}{description&&<p className="mt-0.5 text-xs text-muted">{description}</p>}</div>{action}</div>}<div className="panel-pad">{children}</div></section> }
export function SummaryCard({label,value,detail,icon:Icon,tone='hydro'}:{label:string;value:ReactNode;detail:string;icon?:LucideIcon;tone?:'hydro'|'good'|'warn'|'danger'}) { const tones={hydro:'text-hydro bg-blue-50',good:'text-good bg-emerald-50',warn:'text-warn bg-amber-50',danger:'text-danger bg-red-50'}; return <div className="panel p-3.5"><div className="mb-3 flex items-start justify-between gap-2"><span className="eyebrow text-muted">{label}</span>{Icon&&<span className={`rounded-md p-1.5 ${tones[tone]}`}><Icon size={16} aria-hidden="true"/></span>}</div><div className="metric-value text-ink">{value}</div><p className="mt-2 text-xs text-muted">{detail}</p></div> }
