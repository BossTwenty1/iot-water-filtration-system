import { CartesianGrid,Legend,Line,LineChart,ResponsiveContainer,Tooltip,XAxis,YAxis } from 'recharts'
import { useServiceData } from '../../hooks/useServiceData'
import { telemetryService } from '../../services'
import { ResourceState } from '../common/ResourceState'
export function TelemetryChart({compact=false}:{compact?:boolean}) { const resource=useServiceData(telemetryService.getTelemetryChart); if(resource.status!=='success'||!resource.data)return <ResourceState status={resource.status} error={resource.error} loadingLabel="Loading telemetry chart…" emptyLabel="No telemetry available."/>; return <div className={compact?'h-52 w-full':'chart-box'}><ResponsiveContainer width="100%" height="100%"><LineChart data={resource.data} margin={{top:10,right:12,left:-18,bottom:0}}><CartesianGrid stroke="#dce4ef" vertical={false}/><XAxis dataKey="time" tick={{fontFamily:'JetBrains Mono',fontSize:10,fill:'#0b1c30'}}/><YAxis domain={[0,10]} tick={{fontFamily:'JetBrains Mono',fontSize:10,fill:'#0b1c30'}}/><Tooltip contentStyle={{fontFamily:'JetBrains Mono',fontSize:11,borderColor:'#c9d4e2'}}/><Legend wrapperStyle={{fontFamily:'JetBrains Mono',fontSize:10,color:'#0b1c30'}}/><Line type="monotone" name="Before Filtration" dataKey="before" stroke="#596b7c" strokeDasharray="5 4" strokeWidth={2} dot={false}/><Line type="monotone" name="After Filtration" dataKey="after" stroke="#007bb9" strokeWidth={2.5} dot={{r:2}}/></LineChart></ResponsiveContainer></div> }
